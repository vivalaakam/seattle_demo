export type Book = {
  _id: string;
  title: string;
  author: string;
};

export type Framework = {
  _id: string;
  title: string;
  author: string;
  repository: string;
};

export type Author = {
  _id: string;
  name: string;
}
